API.gnome.start();
/*
var fs = API.fileSystem;
fs.newFile("g10.txt");
fs.write("g10.txt", getGrande());

function getGrande(){
	var r = "";
	var loop = 0;
	while(loop < 7000) {
		for(var i = 32; i < 80; i++) {
			r += String.fromCharCode(i);
		}
		r += "\n";		
		loop++;
	}
	return r;
	
}
*/
